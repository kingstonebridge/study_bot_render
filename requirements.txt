# Core dependencies for your bot logic
python-telegram-bot==21.10
sqlalchemy==2.0.34
apscheduler==3.10.4

# Web hosting and ASGI server
fastapi
gunicorn==23.0.0
uvicorn

# Environment variable management
python-dotenv==1.0.1

# 🛠️ MISSING REQUIREMENT (Fixes ModuleNotFoundError: No module named 'requests')
# Needed for set_webhook_if_needed function.
requests

# (Optional but recommended for robust HTTP connections, especially with async)
# The logs show httpx was installed, but explicitly adding ensures it is available
# for any async HTTP operations outside of ptb's core.
httpx
python-binance
